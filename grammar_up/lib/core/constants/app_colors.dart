import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - Teal/Cyan như thiết kế
  static const Color primary = Color(0xFF4ECDC4);
  static const Color primaryDark = Color(0xFF26A69A);
  static const Color primaryLight = Color(0xFFB2DFDB);

  // Secondary colors
  static const Color secondary = Color(0xFF2C3E50);
  static const Color accent = Color(0xFF1ABC9C);

  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Status colors
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF39C12);
  static const Color error = Color(0xFFE74C3C);

  // Difficulty colors
  static const Color easy = Color(0xFF27AE60);
  static const Color medium = Color(0xFFF39C12);
  static const Color hard = Color(0xFFE74C3C);

  // Other
  static const Color divider = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1A000000);
}
